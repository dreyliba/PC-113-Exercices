const age = 25;
    if(age < 18 ){
        console.log("You can't vote.");
    } else if(age >= 18 && age <= 100) {
        console.log("you can vote.");
    } else {
        console.log("No idea.");
    }